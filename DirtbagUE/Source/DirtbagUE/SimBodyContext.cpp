// Bridge: compiles the engine-free body-context core into the UE module.
// The one place that stamps who a climber is, what their joints carry and
// what they have caught onto an attempt -- see Sim/DirtbagBodyContext.h for
// the measured hole it exists to close.
#include "../../../Sim/DirtbagBodyContext.cpp"
